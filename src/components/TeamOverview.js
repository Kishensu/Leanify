import React from 'react';
import { Typography } from '@mui/material';
import { Box } from '@mui/system';

const TeamOverview = () => {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>
        Lean Six Sigma Team
      </Typography>
      <Typography variant="body1">
        Our team is dedicated to continuous improvement by implementing Lean Six Sigma methodologies to enhance process excellence.
      </Typography>
    </Box>
  );
};

export default TeamOverview;